name: Send Issue message to Discord
on:
  workflow_run:
    workflows: ["Convert Issue to Markdown"]
    types:
      - completed
      
jobs:
  on-success:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - run: echo 'congradulations! your workflow sucessfully completed!'
